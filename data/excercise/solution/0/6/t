Python programming language treats all sting as list, i.e. we can access each character of a string using square brackets.

Suppose the user enters a string
str = "hello"
now,
str[0] will return character at position 0, i.e. 'h'
and str[-1] will return character at -1, i.e. last character 'o'

